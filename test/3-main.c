#include "main.h"

/**
 * main -Entry point, test decimal and integer
 *
 * Return: Always 0 (success)
 */
int main(void)
{
	int found = 2019, curr = 2023;

	printf("%d\n", found);
	new_printf("%d\n", found);
	printf("%i\n", curr);
	new_printf("%i\n", curr);
	return (0);
}

